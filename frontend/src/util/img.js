export default function() {
   // const prefix = `http://localhost:8080/src/img/apt/apt`;
    //const suffix = `.jpg`;
    const num = Math.ceil(Math.random() * 15) + 1;
    return num;
    //return `${prefix}${num}${suffix}`;
}